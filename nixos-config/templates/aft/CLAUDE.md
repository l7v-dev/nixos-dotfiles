@AGENTS.md

<!--
Stack-specific agent rule blocks go below, each wrapped in its own
BEGIN/END marker, e.g.:

<!-- TRIGGER.DEV basic START -->
... trigger.dev v4 usage rules ...
<!-- TRIGGER.DEV basic END -->

Keep each block self-contained so it can be added/removed independently
when a dependency is added to or dropped from the project. Do not hand-edit
generated blocks pulled in by a skill installer — regenerate them instead.
-->

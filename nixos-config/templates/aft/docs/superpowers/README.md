# superpowers/plans

Longer-lived planning documents go here, one per initiative, named
`YYYY-MM-DD-short-name.md`. Unlike `context/feature-specs`, these can span
multiple features and are allowed to go stale — they're a record of intent,
not a source of truth for current behavior (that's what `context/` is for).

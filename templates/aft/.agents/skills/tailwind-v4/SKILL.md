# Skill: TailwindCSS v4 Standard

## Kurallar
- CSS değişkenlerini `globals.css` içinde `@import "tailwindcss";` direktifi ile kullanın.
- Karmaşık sınıf kombinasyonları için `src/lib/utils.ts` içindeki `cn()` yardımcısını çağırın.
- Dark mode uyumluluğu için `dark:` varyantlarını kullanın.

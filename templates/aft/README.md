# __PROJECT_TITLE__

Scaffolded from the AFT (Agentic Framework Template).

## Getting started

```bash
npm install
cp .env.example .env   # fill in as dependencies are added
npm run dev
```

## Agent workflow

This project follows a spec-driven, context-file-first workflow — see
`AGENTS.md` for the read order and `context/ai-workflow-rules.md` for the
rules. Start by filling in the `TODO`s in:

1. `context/project-overview.md`
2. `context/architecture-context.md`
3. `context/ui-context.md`

before implementing anything.
